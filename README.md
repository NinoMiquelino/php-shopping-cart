# php-shopping-cart
Sistema de Carrinho de Compras em PHP. Usa a classe CartManager (POO) para gerenciar o estado na $_SESSION e persistir o carrinho. O frontend usa AJAX para operações CRUD em tempo real.
